import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customers',
  description: 'Manage customer profiles, view interactions, and track engagement.',
};
export default function CustomersPage() {
    return <p>Customers Page</p>;
}